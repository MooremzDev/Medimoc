{
  "name": "medimoc",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack Primavera SQL Server dashboard with Express and React.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,green \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "start:prod": "npm run build --workspace frontend && npm run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
